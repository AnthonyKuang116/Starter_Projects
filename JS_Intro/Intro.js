const name = "Anthony";

console.log(name[0]);

//Note: hasClass returns a boolean...works well with if/else statements
//Others notes: addClass, removeClass, toggleClass
//for toggleClass, it gives it a defined class if it doesn't have one. If it does have the class then it takes the class away